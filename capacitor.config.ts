import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.findjob.sixsigmastudy',
  appName: 'Six Sigma Study',
  webDir: 'apps/reader/dist'
};

export default config;
