import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.findjob.sixsigmastudy',
  appName: '飞阅',
  webDir: 'apps/reader/dist'
};

export default config;
